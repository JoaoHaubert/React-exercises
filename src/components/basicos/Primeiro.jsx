import React from "react";

export default () => {

    const msg = 'Bem vindo(a)!'

    return (
        <div>
            <h2>Primeiro Componente</h2>
            <p>{ msg }</p>
        </div>
    )
}

